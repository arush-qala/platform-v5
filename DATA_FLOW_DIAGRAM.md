# 🌊 DATA FLOW VISUALIZATION - QALA PLATFORM

## 📱 **USER JOURNEY: BROWSING TO ORDERING**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                         👤 USER (Boutique Owner)                            │
│                                                                             │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 │ Types: www.qala.com
                                 ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│                    🌐 BROWSER (Chrome, Safari, etc.)                        │
│                                                                             │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 │ HTTP Request
                                 ↓
╔═════════════════════════════════════════════════════════════════════════════╗
║                                                                             ║
║                      ⚡ VERCEL HOSTING (Cloud Server)                       ║
║                                                                             ║
╚════════════════════════════════╦════════════════════════════════════════════╝
                                 │
                                 │ Forwards request
                                 ↓
╔═════════════════════════════════════════════════════════════════════════════╗
║                       🏗️ NEXT.JS APPLICATION                                ║
║                     (The Brain of Your Platform)                           ║
║─────────────────────────────────────────────────────────────────────────────║
║                                                                             ║
║  ┌─────────────────────────────────────────────────────────────────┐       ║
║  │  📍 ROUTING LAYER                                               │       ║
║  │  Decides which page to show based on URL                        │       ║
║  │                                                                 │       ║
║  │  /                      → Homepage                              │       ║
║  │  /discover              → Discovery Page                        │       ║
║  │  /brands/maison-solene  → Brand Store Page                     │       ║
║  │  /api/brands            → API Endpoint (data only)             │       ║
║  └─────────────────────────────────────────────────────────────────┘       ║
║                                 │                                           ║
║                                 ↓                                           ║
║  ┌─────────────────────────────────────────────────────────────────┐       ║
║  │  ⚛️ REACT COMPONENTS                                             │       ║
║  │  Building blocks that create the visual interface              │       ║
║  │                                                                 │       ║
║  │  Components Used:                                               │       ║
║  │  • BrandTimeline.tsx    → The horizontal brand browser          │       ║
║  │  • ProductCarousel.tsx  → Instagram-style product viewer        │       ║
║  │  • ChatPanel.tsx        → Messaging interface                   │       ║
║  │  • VirtualTryOn.tsx     → 3D try-on feature                     │       ║
║  └─────────────────────────────────────────────────────────────────┘       ║
║                                 │                                           ║
║                                 ↓                                           ║
║  ┌─────────────────────────────────────────────────────────────────┐       ║
║  │  🎨 STYLING LAYER (Tailwind CSS)                                │       ║
║  │  Applies luxury design: colors, fonts, spacing                 │       ║
║  └─────────────────────────────────────────────────────────────────┘       ║
║                                 │                                           ║
║                                 ↓                                           ║
║  ┌─────────────────────────────────────────────────────────────────┐       ║
║  │  ✨ ANIMATION LAYER (Framer Motion, GSAP)                       │       ║
║  │  Adds smooth transitions, scroll effects, hover animations     │       ║
║  └─────────────────────────────────────────────────────────────────┘       ║
║                                                                             ║
╚═════════════════════════════════════════════════════════════════════════════╝
```

---

## 🔄 **SCENARIO 1: USER VIEWS BRAND PAGE**

```
┌────────────┐
│   USER     │  Clicks "Maison Solène" brand
└─────┬──────┘
      │
      ↓
┌────────────────────────────────────────────┐
│  BROWSER                                   │
│  Sends: GET /brands/maison-solene          │
└─────┬──────────────────────────────────────┘
      │
      ↓
┌──────────────────────────────────────────────────────────────┐
│  NEXT.JS ROUTER                                              │
│  "Ah, they want a brand page!"                               │
│  Looks for: app/brands/[slug]/page.tsx                       │
└─────┬────────────────────────────────────────────────────────┘
      │
      ↓
┌──────────────────────────────────────────────────────────────┐
│  PAGE COMPONENT (brands/[slug]/page.tsx)                     │
│  "I need brand data. Let me fetch it!"                       │
└─────┬────────────────────────────────────────────────────────┘
      │
      │ Calls Internal Function
      ↓
┌──────────────────────────────────────────────────────────────┐
│  PRISMA CLIENT (lib/prisma.ts)                               │
│  "I'll translate that to database language"                  │
│                                                              │
│  Code: prisma.brand.findUnique({                             │
│           where: { slug: 'maison-solene' }                   │
│         })                                                   │
└─────┬────────────────────────────────────────────────────────┘
      │
      │ SQL Query: SELECT * FROM Brand WHERE slug='maison-solene'
      ↓
┌──────────────────────────────────────────────────────────────┐
│  💾 POSTGRESQL DATABASE                                      │
│  Searches through Brand table                                │
│  Finds the record                                            │
│                                                              │
│  Returns:                                                    │
│  {                                                           │
│    id: "cm5abc123",                                          │
│    name: "Maison Solène",                                    │
│    slug: "maison-solene",                                    │
│    description: "Contemporary Parisian elegance...",         │
│    location: "Paris, France",                                │
│    logoUrl: "/images/brands/maison-solene/logo.jpg",         │
│    collections: [...],                                       │
│    ...                                                       │
│  }                                                           │
└─────┬────────────────────────────────────────────────────────┘
      │
      │ Returns Data Object
      ↓
┌──────────────────────────────────────────────────────────────┐
│  PAGE COMPONENT                                              │
│  Receives data, passes it to React components                │
└─────┬────────────────────────────────────────────────────────┘
      │
      ↓
┌──────────────────────────────────────────────────────────────┐
│  REACT RENDERS HTML                                          │
│  • Displays brand name                                       │
│  • Shows logo image                                          │
│  • Lists collections                                         │
│  • Renders lookbook gallery                                  │
│  • Adds animations                                           │
└─────┬────────────────────────────────────────────────────────┘
      │
      │ Sends HTML + CSS + JavaScript
      ↓
┌──────────────────────────────────────────────────────────────┐
│  BROWSER                                                     │
│  Displays beautiful brand page to user                       │
└──────────────────────────────────────────────────────────────┘
      │
      ↓
┌────────────┐
│   USER     │  Sees: Maison Solène brand page with images,
└────────────┘         collections, and interactive elements
```

**⏱️ This entire flow takes: 200-500ms (less than half a second!)**

---

## 🛒 **SCENARIO 2: USER ADDS PRODUCT TO INQUIRY**

```
┌────────────┐
│   USER     │  Selects size M, quantity 10
└─────┬──────┘  Clicks "Add to Inquiry"
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  REACT COMPONENT (ProductDetails.tsx)                       │
│  • Stores selection in component state                      │
│  • Updates UI to show "Added ✓"                             │
│  • Keeps data in browser memory (for now)                   │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ User clicks "Send Inquiry"
      ↓
┌─────────────────────────────────────────────────────────────┐
│  REACT COMPONENT                                            │
│  Prepares data to send to server:                           │
│                                                             │
│  {                                                          │
│    productId: "cm5xyz789",                                  │
│    size: "M",                                               │
│    quantity: 10,                                            │
│    customization: "Add longer sleeves",                     │
│    userId: "cm5user123"                                     │
│  }                                                          │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ POST request via fetch() or axios
      ↓
┌─────────────────────────────────────────────────────────────┐
│  API ROUTE (app/api/inquiry/route.ts)                       │
│  Receives the data                                          │
│  Validates: Is user logged in? Is product available?        │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ If valid, save to database
      ↓
┌─────────────────────────────────────────────────────────────┐
│  PRISMA CLIENT                                              │
│  prisma.customization.create({                              │
│    data: {                                                  │
│      productId: "cm5xyz789",                                │
│      userId: "cm5user123",                                  │
│      description: "Size M, Qty 10, Add longer sleeves",     │
│      status: "pending"                                      │
│    }                                                        │
│  })                                                         │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ Translates to SQL: INSERT INTO Customization...
      ↓
┌─────────────────────────────────────────────────────────────┐
│  💾 POSTGRESQL DATABASE                                     │
│  Saves the record permanently                               │
│  Returns: { id: "cm5custom456", status: "pending", ... }    │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ Success confirmation
      ↓
┌─────────────────────────────────────────────────────────────┐
│  API ROUTE                                                  │
│  Returns success: { success: true, inquiryId: "..." }       │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ Response sent back
      ↓
┌─────────────────────────────────────────────────────────────┐
│  REACT COMPONENT                                            │
│  Receives success message                                   │
│  Shows: "Inquiry sent successfully! ✓"                      │
│  Maybe sends user to confirmation page                      │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌────────────┐
│   USER     │  Sees success message
└────────────┘  Receives email confirmation (future feature)
```

---

## 💬 **SCENARIO 3: REAL-TIME CHAT MESSAGE**

```
┌────────────┐
│   USER     │  Types: "Do you ship to Dubai?"
└─────┬──────┘  Presses Enter
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  CHAT COMPONENT (components/chat/ChatPanel.tsx)             │
│  Captures message text                                      │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ POST /api/messages
      ↓
┌─────────────────────────────────────────────────────────────┐
│  API ROUTE (app/api/messages/route.ts)                      │
│  Receives: {                                                │
│    brandId: "cm5brand123",                                  │
│    userId: "cm5user456",                                    │
│    content: "Do you ship to Dubai?",                        │
│    timestamp: "2025-11-24T10:30:00Z"                        │
│  }                                                          │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  PRISMA CLIENT                                              │
│  prisma.message.create({ data: {...} })                     │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  💾 POSTGRESQL - MESSAGE TABLE                              │
│  Saves message permanently                                  │
│                                                             │
│  [Messages Table]                                           │
│  ┌────────┬──────────┬──────────┬─────────────────┬─────┐  │
│  │   ID   │ BrandID  │  UserID  │    Content      │Time │  │
│  ├────────┼──────────┼──────────┼─────────────────┼─────┤  │
│  │ msg001 │ brand123 │ user456  │ "Do you ship.." │10:30│  │
│  └────────┴──────────┴──────────┴─────────────────┴─────┘  │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ ✅ Saved successfully
      ↓
┌─────────────────────────────────────────────────────────────┐
│  API ROUTE                                                  │
│  Returns: { success: true, messageId: "msg001" }            │
│                                                             │
│  🔔 (Future) Triggers:                                      │
│      • Email notification to brand                          │
│      • Push notification                                    │
│      • WebSocket broadcast (real-time update)               │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  CHAT COMPONENT                                             │
│  Adds message to chat window                                │
│  Shows timestamp and checkmark                              │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌────────────┐
│   USER     │  Sees message appear in chat
└────────────┘
      ↕️
      ⏰ Meanwhile, on brand's computer...
      ↕️
┌────────────┐
│   BRAND    │  Chat window updates
└────────────┘  Sees: "New message from user456"
                (Currently simulated, will be real-time with WebSockets)
```

---

## 🗄️ **DATABASE STRUCTURE & RELATIONSHIPS**

```
                    ┌─────────────────────────┐
                    │       👔 BRAND          │
                    │─────────────────────────│
                    │ id                      │
                    │ name: "Maison Solène"   │
                    │ slug: "maison-solene"   │
                    │ description             │
                    │ logoUrl                 │
                    │ location: "Paris"       │
                    └───────────┬─────────────┘
                                │
                                │ One brand has many collections
                                │ One brand has many messages
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ↓               ↓               ↓
    ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
    │ 📚 COLLECTION   │  │ 💬 MESSAGE      │  │ 🏆 BRAND        │
    │─────────────────│  │─────────────────│  │    FEATURE      │
    │ id              │  │ id              │  │─────────────────│
    │ brandId ────────┼──→ brandId         │  │ id              │
    │ name: "SS 2024" │  │ userId          │  │ brandId ────────┼─→ Links to Brand
    │ season: "Spring"│  │ content: "..."  │  │ title: "Vogue"  │
    │ lookbookImages  │  │ timestamp       │  │ publication     │
    └────────┬────────┘  │ read: false     │  │ url             │
             │           └─────────────────┘  └─────────────────┘
             │
             │ One collection has many products
             │
             ↓
    ┌─────────────────────────┐
    │    👗 PRODUCT           │
    │─────────────────────────│
    │ id                      │
    │ collectionId ───────────┼─→ Links to Collection
    │ name: "Silk Maxi Dress" │
    │ category: "dresses"     │
    │ price: 1200.00          │
    │ fabricDetails           │
    │ colors: ["Navy","Cream"]│
    └────────┬────────────────┘
             │
             │ One product has many...
             │
    ┌────────┼────────┬─────────────────┐
    │        │        │                 │
    ↓        ↓        ↓                 ↓
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐
│ 📷 IMAGE│ │ 📏 SIZE │ │ 🎨 CUSTOM│ │ Data for each:  │
│─────────│ │─────────│ │─────────│ │                 │
│productId│ │productId│ │productId│ │ Images: URLs    │
│url      │ │size: "M"│ │userId   │ │ Sizes: M, L, XL │
│order: 1 │ │inStock  │ │request  │ │ Custom: Special │
│isPrimary│ │quantity │ │status   │ │         requests│
└─────────┘ └─────────┘ └─────────┘ └─────────────────┘
```

---

## 🔐 **SECURITY & AUTHENTICATION FLOW** (Future Implementation)

```
┌────────────┐
│   USER     │  Enters email + password
└─────┬──────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  LOGIN FORM (React Component)                               │
│  Captures: { email: "buyer@boutique.com", password: "***" } │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ POST /api/auth/login
      ↓
┌─────────────────────────────────────────────────────────────┐
│  API ROUTE (app/api/auth/login/route.ts)                    │
│  Step 1: Check if user exists                               │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  PRISMA → DATABASE                                          │
│  Query: SELECT * FROM User WHERE email='buyer@boutique.com' │
│  Returns: { id, email, name, passwordHash }                 │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  BCRYPT LIBRARY                                             │
│  Compares entered password with stored hash                 │
│                                                             │
│  Entered: "mypassword123"                                   │
│  Stored:  "$2b$10$KixJ8uWT.../..." (encrypted)              │
│                                                             │
│  bcrypt.compare(password, hash) → ✅ TRUE or ❌ FALSE        │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ ✅ Password matches!
      ↓
┌─────────────────────────────────────────────────────────────┐
│  JSONWEBTOKEN LIBRARY                                       │
│  Creates secure token (like a digital passport)             │
│                                                             │
│  jwt.sign({                                                 │
│    userId: "cm5user456",                                    │
│    email: "buyer@boutique.com",                             │
│    role: "buyer"                                            │
│  }, SECRET_KEY, { expiresIn: "7d" })                        │
│                                                             │
│  Returns: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQi │
│            OiJjbTV1c2VyNDU2IiwiZW1haWwiOiJidXllckBib3V0aXF1ZS │
│            ..." (encrypted token)                            │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ Sends token back
      ↓
┌─────────────────────────────────────────────────────────────┐
│  API RESPONSE                                               │
│  { success: true, token: "eyJhbGci...", user: {...} }       │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  BROWSER                                                    │
│  Stores token in:                                           │
│    • localStorage (persists after closing browser)          │
│    • Cookie (sent automatically with each request)          │
│    • React state (for current session)                      │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌────────────┐
│   USER     │  Now logged in! ✅
└────────────┘  All future requests include token

─────────────────────────────────────────────────────────────

FUTURE REQUESTS:

Every time user makes a request, the token is verified:

┌────────────┐
│   USER     │  Clicks "View Orders"
└─────┬──────┘
      │ Sends: { token: "eyJhbGci..." }
      ↓
┌─────────────────────────────────────────────────────────────┐
│  API MIDDLEWARE                                             │
│  jwt.verify(token, SECRET_KEY)                              │
│  Checks: Is this token valid? Has it expired?               │
│                                                             │
│  ✅ Valid → Extract userId, proceed to database             │
│  ❌ Invalid → Return 401 Unauthorized                       │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 **PERFORMANCE OPTIMIZATION FLOW**

```
┌────────────┐
│   USER     │  Visits homepage
└─────┬──────┘  Location: India
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  🌐 CLOUDFLARE CDN (Future)                                 │
│  "Do I have a cached version of this page?"                 │
│                                                             │
│  ✅ Yes → Serve from Mumbai server (10ms)                   │
│  ❌ No  → Fetch from origin server (200ms)                  │
└─────┬───────────────────────────────────────────────────────┘
      │
      │ If cache miss
      ↓
┌─────────────────────────────────────────────────────────────┐
│  ⚡ VERCEL EDGE NETWORK                                     │
│  Serves from nearest server location                        │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  NEXT.JS SERVER                                             │
│  Uses built-in optimizations:                               │
│                                                             │
│  1️⃣ Server-Side Rendering (SSR)                             │
│     Pre-renders HTML on server → Faster first load         │
│                                                             │
│  2️⃣ Static Generation (SSG)                                 │
│     Pre-builds pages at build time → Ultra-fast            │
│                                                             │
│  3️⃣ Image Optimization                                      │
│     Automatically resizes, compresses, lazy-loads images    │
│                                                             │
│  4️⃣ Code Splitting                                          │
│     Only loads JavaScript needed for current page          │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│  BROWSER RECEIVES                                           │
│  • Optimized HTML (small file size)                         │
│  • Compressed CSS (Tailwind purged unused styles)           │
│  • Minimal JavaScript (only what's needed)                  │
│  • Lazy-loaded images (load as user scrolls)                │
└─────┬───────────────────────────────────────────────────────┘
      │
      ↓
┌────────────┐
│   USER     │  Sees fully loaded page in < 2 seconds ⚡
└────────────┘
```

---

## 🔄 **COMPLETE REQUEST-RESPONSE CYCLE**

```
     USER ACTION                    SYSTEM RESPONSE
     ───────────                    ───────────────

1️⃣   Types URL                      DNS lookup
     www.qala.com          →        Finds server IP address
                                    Returns: 76.76.21.21

2️⃣   Browser connects               SSL/TLS Handshake
     to server             →        Establishes secure connection (HTTPS)
                                    🔒 Encrypted tunnel created

3️⃣   Sends HTTP request             Routing
     GET /brands/          →        Next.js Router catches request
     maison-solene                  Matches to: app/brands/[slug]/page.tsx

4️⃣   Server processes               Data Fetching
                          →        Prisma queries PostgreSQL
                                    Gets brand + collections + products

5️⃣   Data retrieved                 React Rendering
                          →        Components render with data
                                    HTML/CSS/JS generated

6️⃣   Response sent                  Browser Receives
                          →        Parses HTML
                                    Applies CSS styles
                                    Executes JavaScript

7️⃣   Assets loaded                  Final Rendering
                          →        Downloads images
                                    Loads fonts
                                    Initializes animations

8️⃣   Page interactive               Ready to Use
                          →        User can click, scroll, interact
                                    ✅ Page fully loaded

     ⏱️ Total time: 500ms - 2s
```

---

## 🎯 **KEY TAKEAWAYS**

### ✅ **Data Always Flows:**
```
User → Browser → Server → Database → Server → Browser → User
```

### ✅ **Three Types of Data Flow:**

1. **📖 READ (Viewing data)**
   - Fast, cached, no changes to database
   - Examples: Viewing products, browsing brands

2. **✍️ WRITE (Saving data)**
   - Slower, validates data, writes to database
   - Examples: Sending message, creating order

3. **🔄 UPDATE (Changing data)**
   - Fetches current data, modifies it, saves back
   - Examples: Updating profile, changing order status

### ✅ **Security Layers:**

```
1. HTTPS          → Encrypts data in transit
2. Authentication → Verifies user identity
3. Authorization  → Checks user permissions
4. Input Validation → Prevents malicious data
5. Database Rules → Enforces data integrity
```

---

## 📱 **MOBILE APP FLOW** (Future)

When you build a mobile app (React Native), the flow is similar:

```
┌─────────────────┐
│  📱 MOBILE APP  │  React Native (iOS/Android)
└────────┬────────┘
         │
         │ Same API endpoints!
         │ POST /api/brands, GET /api/products
         │
         ↓
┌─────────────────┐
│  🌐 YOUR API    │  Next.js API Routes
└────────┬────────┘  (No changes needed!)
         │
         ↓
┌─────────────────┐
│  💾 DATABASE    │  PostgreSQL
└─────────────────┘  (Same data source!)
```

**This is called "API-First Architecture"** → Build once, use everywhere!

---

## 🎓 **SIMPLIFIED MENTAL MODEL**

Think of your platform like a restaurant:

| Restaurant Part | Your Platform | What It Does |
|----------------|---------------|--------------|
| 🏪 **Storefront** | Frontend (React/Next.js) | What customers see |
| 📋 **Menu** | Pages & Components | Available options |
| 🔔 **Waiter** | API Routes | Takes orders, brings food |
| 👨‍🍳 **Kitchen** | Backend Logic | Processes requests |
| 🗄️ **Pantry** | Database (PostgreSQL) | Stores ingredients (data) |
| 📦 **Suppliers** | External APIs (Stripe, Email) | Third-party services |
| 🚚 **Delivery** | Hosting (Vercel) | Gets food to customers |

---

## 📍 **WHERE DATA LIVES**

```
┌──────────────────────────────────────────────────────────────┐
│  CLIENT-SIDE (User's Browser)                                │
│  • Temporary data: Form inputs, current selections           │
│  • Cached data: Previously loaded pages                      │
│  • Local storage: Auth tokens, preferences                   │
│  • NOT SECURE: Anyone can inspect                            │
│  ⚠️ Never store sensitive data here!                          │
└──────────────────────────────────────────────────────────────┘
                             ↕️
                    (Internet / HTTPS)
                             ↕️
┌──────────────────────────────────────────────────────────────┐
│  SERVER-SIDE (Vercel/Your Hosting)                           │
│  • Application code: Next.js, React components               │
│  • Business logic: Validation, calculations                  │
│  • API endpoints: Process requests                           │
│  • Environment variables: API keys, secrets                  │
│  ✅ Secure, but stateless (doesn't remember between requests)│
└──────────────────────────────────────────────────────────────┘
                             ↕️
                    (Database Connection)
                             ↕️
┌──────────────────────────────────────────────────────────────┐
│  DATABASE (PostgreSQL on Vercel/AWS)                         │
│  • PERMANENT storage: All brands, products, users, orders    │
│  • Survives crashes: Data persists forever (until deleted)   │
│  • MUST be backed up: Your entire business depends on this   │
│  🔐 Most secure layer: Only server can access                │
└──────────────────────────────────────────────────────────────┘
```

---









